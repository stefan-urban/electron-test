
name: Build package

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - run: npm install
    - run: npm run dist-linux
    - uses: actions/upload-artifact@v4
      with:
        name: linux-zip
        path: dist/